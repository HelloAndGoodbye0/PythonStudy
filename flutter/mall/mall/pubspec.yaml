name: mall
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0          # 状态管理
  cached_network_image: ^3.2.0 # 图片缓存
  intl: ^0.18.0             # 货币格式化
  flutter_staggered_grid_view: ^0.7.0 # 瀑布流布局(可选，这里暂用原生Grid)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
